{
  "name": "personal-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx scripts/build-posts.ts && vite",
    "build:posts": "tsx scripts/build-posts.ts",
    "build": "npm run build:posts && tsc && vite build && cp docs/CNAME dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "front-matter": "^4.0.2",
    "marked": "^15.0.0",
    "tsx": "^4.0.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "wired-elements": "^3.0.0-rc.6"
  }
}
